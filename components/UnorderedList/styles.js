import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  listItemContainer: {
    flex: 1,
    flexDirection: 'row',
  },
  bullet: {
    marginRight: 5,
  },
});

export default styles;
